---
trigger: always_on
---

If you cannot complete a task after exhausting the allowed attempts, your final response MUST follow this exact format:
1. WHAT I TRIED: A brief summary of the failed attempts.
2. WHY IT FAILED: The technical root cause (not just the symptom).
3. USER OPTIONS: Present 2-3 alternative paths for the user to decide (e.g., 'Force action', 'Manual override', 'Change parameters').
WAIT for user instruction before proceeding.